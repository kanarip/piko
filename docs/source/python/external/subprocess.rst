==============
``subprocess``
==============

The :py:mod:`subprocess` module is used to execute external processes via the
|product_name| API, such as starting a **Redis** server and **Celery** workers
when executing tests, or starting **Gammu** to send text messages.

.. automodule:: subprocess
